from functools import total_ordering


@total_ordering
class Student:
    
    def __init__(self, name, surname, gender):
        self.name = name
        self.surname = surname
        self.gender = gender
        self.finished_courses = []
        self.courses_in_progress = []
        self.grades = {}

    def rate_lecturer(self, lecturer, course, grade):
        if (isinstance(lecturer, Lecturer) and
            course in self.courses_in_progress and
            course in lecturer.courses_attached):
            if 1 <= grade <= 10:
                if course in lecturer.grades:
                    lecturer.grades[course] += [grade]
                else:
                    lecturer.grades[course] = [grade]
            else:
                return 'Ошибка: оценка должна быть от 1 до 10'
        else:
            return 'Ошибка'

    def _get_average_grade(self):
        if not self.grades:
            return 0.0
        all_grades = []
        for grades_list in self.grades.values():
            all_grades.extend(grades_list)
        return round(sum(all_grades) / len(all_grades), 1)

    def __str__(self):
        avg_grade = self._get_average_grade()
        in_progress = ", ".join(self.courses_in_progress)
        finished = ", ".join(self.finished_courses)
        return(f"Имя: {self.name}\n"
               f"Фамилия: {self.surname}\n"
               f"Средняя оценка за домашнее задание: {avg_grade}\n"
               f"Курсы в процессе изучения: {in_progress}\n"
               f"Завершенные курсы: {finished}")

    def __lt__(self, other):
        if not isinstance(other, Student):
            return NotImplemented
        return self._get_average_grade() < other._get_average_grade()

    def __eq__(self, other):
        if not isinstance(other, Student):
            return NotImplemented
        return self._get_average_grade() == other._get_average_grade()


class Mentor:

    def __init__(self, name, surname):
        self.name = name
        self.surname = surname
        self.courses_attached = []

    def rate_hw(self, student, course, grade):
        if isinstance(student, Student) and course in self.courses_attached and course in student.courses_in_progress:
            if course in student.grades:
                student.grades[course] += [grade]
            else:
                student.grades[course] = [grade]
        else:
            return 'Ошибка'


@total_ordering
class Lecturer(Mentor):
    
    def __init__(self, name, surname):
        super().__init__(name, surname)
        self.grades = {}

    def _get_average_grade(self):
        if not self.grades:
            return 0.0
        all_grades = []
        for grades_list in self.grades.values():
            all_grades.extend(grades_list)
        return round(sum(all_grades) / len(all_grades), 1) 

    def __str__(self):
        avg_grade = self._get_average_grade()
        return f"Имя: {self.name}\nФамилия: {self.surname}\nСредняя оценка за лекции: {avg_grade}"

    def __lt__(self, other):
        if not isinstance(other, Lecturer):
            return NotImplemented
        return self._get_average_grade() < other._get_average_grade()

    def __eq__(self, other):
        if not isinstance(other, Lecturer):
            return NotImplemented
        return self._get_average_grade() == other._get_average_grade()


class Reviewer(Mentor):
    
    def __init__(self, name, surname):
        super().__init__(name, surname)

    def rate_hw(self, student, course, grade):
        if isinstance(student, Student) and course in self.courses_attached and course in student.courses_in_progress:
            if course in student.grades:
                student.grades[course] += [grade]
            else:
                student.grades[course] = [grade]
        else:
            return 'Ошибка'

    def __str__(self):
        return f"Имя: {self.name}\nФамилия: {self.surname}"


def get_avg_students_grade(students_list, course_name):
    """Считает среднюю оценку за ДЗ по всем студентам на конкретном курсе."""
    all_grades = []
    for student in students_list:
        if course_name in student.grades:
            all_grades.extend(student.grades[course_name])
    
    if not all_grades:
        return 0.0
    return round(sum(all_grades) / len(all_grades), 1)

def get_avg_lecturers_grade(lecturers_list, course_name):
    """Считает среднюю оценку за лекции всех лекторов на конкретном курсе."""
    all_grades = []
    for lecturer in lecturers_list:
        if course_name in lecturer.grades:
            all_grades.extend(lecturer.grades[course_name])
    
    if not all_grades:
        return 0.0
    return round(sum(all_grades) / len(all_grades), 1)



student_1 = Student('Cowboy', 'Marlboro', 'M')
student_2 = Student('Harley', 'Davidson', 'M')

lecturer_1 = Lecturer('Ramsay', 'Bolton')
lecturer_2 = Lecturer('Jon', 'Snow')

reviwer_1 = Reviewer('Rob', 'Zombie')
reviwer_2 = Reviewer('Hans', 'Giger')

student_1.courses_in_progress += ['Python', 'Git']
student_1.finished_courses += ['Введение в программирование']

student_2.courses_in_progress += ['Python']
student_2.finished_courses += ['Git']

lecturer_1.courses_attached += ['Python']
lecturer_2.courses_attached += ['Python', 'Git']

reviwer_1.courses_attached += ['Python']
reviwer_2.courses_attached += ['Git']

reviwer_1.rate_hw(student_1, 'Python', 8)
reviwer_1.rate_hw(student_1, 'Python', 10)
reviwer_1.rate_hw(student_2, 'Python', 9)
reviwer_1.rate_hw(student_2, 'Python', 7)

student_1.rate_lecturer(lecturer_1, 'Python', 10)
student_1.rate_lecturer(lecturer_2, 'Python', 10)
student_1.rate_lecturer(lecturer_2, 'Git', 10)
student_2.rate_lecturer(lecturer_1, 'Python', 10)


print('\nSTUDENTS')
print(student_1)
print(student_2)
print()
print('\nLECTURERS')
print(lecturer_1)
print(lecturer_2)
print()
print('\nREVIWERS')
print(reviwer_1)
print(reviwer_2)
print('\nCOMPARISONS')
print(f'student_1 > student_2: {student_1 > student_2}')
print(f'lecturer_1 == lecturer_2: {lecturer_1 == lecturer_2}')
print()
students_course = [student_1, student_2]
lecturers_course = [lecturer_1, lecturer_2]

print('\nСРЕДНИЕ ОЦЕНКИ ПО КУРСУ')
avg_st_python = get_avg_students_grade(students_course, 'Python')
print(f'Средняя оценка студентов по курсу "Python": {avg_st_python}')

avg_lec_python = get_avg_lecturers_grade(lecturers_course, 'Python')
print(f'Средняя оценка лекторов по курсу "Python": {avg_lec_python}')
